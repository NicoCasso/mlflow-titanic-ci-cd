name: Autre_Fichier_Yml
run-name: essai

on:
  push:
    branches:
      - develop_ubuntu

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3 add .
