name: Autre_Fichier_Yml
run-name: essai

on: 
  workflow_run:
    workflows: ["CI Nico"]
    types: [completed]
    branches:
      - "develop_ubuntu"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3 add .
